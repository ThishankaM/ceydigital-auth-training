# Base URL of the auth API, without a trailing slash (for example
# http://localhost:3000/api). Leave it empty to use the built-in mock API.
VITE_API_URL=

# Force the mock API on or off: "true" | "false".
# When this is empty, mocks are used only if VITE_API_URL is empty.
VITE_USE_MOCKS=true
